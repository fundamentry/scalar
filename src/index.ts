import * as Scalar from '#project/scalar';

export { Scalar };

export const codePoint = Scalar.CodePoint.of;
export const hexadecimal = Scalar.Hexadecimal.of;
export const number = Scalar.Number.of;
